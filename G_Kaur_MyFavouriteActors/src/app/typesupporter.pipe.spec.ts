import { TypesupporterPipe } from './typesupporter.pipe';

describe('TypesupporterPipe', () => {
  it('create an instance', () => {
    const pipe = new TypesupporterPipe();
    expect(pipe).toBeTruthy();
  });
});
